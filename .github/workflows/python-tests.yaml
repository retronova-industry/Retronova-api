name: Python API Tests

on: 
  push:

jobs:
  python-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Set environment variables for tests
        run: |
          echo "DATABASE_URL=postgresql://test_user:test_password@localhost:5432/test_db" >> $GITHUB_ENV
          echo "SECRET_KEY=test-secret-key-for-github-actions" >> $GITHUB_ENV
          echo "ARCADE_API_KEY=test-arcade-key" >> $GITHUB_ENV
          echo "FIREBASE_USER_CREDENTIALS_PATH=./tests/firebase-user-credentials.json" >> $GITHUB_ENV
          echo "FIREBASE_ADMIN_CREDENTIALS_PATH=./tests/firebase-admin-credentials.json" >> $GITHUB_ENV
        
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Run tests
        run: pytest tests -v
